<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:focusable="true"
    android:focusableInTouchMode="true"
    android:orientation="vertical"
    android:gravity="center"
    android:background="@android:color/holo_blue_light">

    <Button
        android:id="@+id/launch_activity_button"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Launch Activity 1"
        android:focusable="true"
        android:focusableInTouchMode="true"
        android:layout_marginBottom="24dp"
        android:paddingHorizontal="16dp"
        android:background="@drawable/focusable_background" />

    <Button
        android:id="@+id/launch_video_activity_button"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Launch Video Activity"
        android:focusable="true"
        android:focusableInTouchMode="true"
        android:layout_marginBottom="24dp"
        android:paddingHorizontal="16dp"
        android:background="@drawable/focusable_background" />

    <Button
        android:id="@+id/launch_apps_activity_button"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Launch Apps Activity"
        android:focusable="true"
        android:focusableInTouchMode="true"
        android:layout_marginBottom="24dp"
        android:paddingHorizontal="16dp"
        android:background="@drawable/focusable_background" />

    <TextView
        android:id="@+id/fragment_tab_1_content"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Content for Tab 1"
        android:textSize="24sp"/>

</LinearLayout>
